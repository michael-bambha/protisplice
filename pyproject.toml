[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "protisplice"
version = "0.1.0"
# Add other metadata like authors, description, etc.
readme = "README.md"
# requires-python = ">=3.8" # Specify your Python version support
# dependencies = [
#     # "some_dependency",
# ]

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["extract_splice_seqs"]